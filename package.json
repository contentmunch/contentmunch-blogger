{
  "name": "contentmunch-blogger",
  "version": "0.1.0",
  "homepage": "https://blogger.contentmunch.com",
  "private": true,
  "dependencies": {
    "@contentmunch/contentmunch-blog": "^0.1.1",
    "@contentmunch/muncher": "^0.6.31"
  },
  "scripts": {
    "start": "export PORT=3010 && react-scripts start ",
    "prebuild": "rimraf docs",
    "build": "react-scripts build && mv build docs",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
